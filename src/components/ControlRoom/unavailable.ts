// Mirrors api/_lib/credentials.ts — kept as a plain client-side constant so
// this component never imports server-only code into the browser bundle.
export const CREDENTIAL_UNAVAILABLE_MESSAGE =
  "Credencial temporariamente indisponível. Contacte o responsável pelo sistema.";
